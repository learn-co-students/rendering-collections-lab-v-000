<h2>Open Orders</h2>
<table>
  <tr>
    <th>Order Id</th>
    <th>Product
    <th>Price
    <th>Customer</th>
  </tr>
  <%#lets partial a collection%>
  <%= render(@orders) || "No Orders"%>
  <%#= render :partial => "order", :collection => @orders || "No Orders"%>
</table>
