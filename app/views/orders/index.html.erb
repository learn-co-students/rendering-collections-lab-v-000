<% if @orders.class == Array && @orders.empty? %>
  <h2>No Orders</h2>
<% end %>

<h2>Open Orders</h2>
<table>
  <tr>
    <th>Order Id</th>
    <th>Product
    <th>Price
    <th>Customer</th>
  </tr>
<%= render @orders %>
</table>
