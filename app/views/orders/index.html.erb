<h2>Open Orders</h2>
<table>
  <tr>
    <th>Order Id</th>
    <th>Product
    <th>Price
    <th>Customer</th>
  </tr>
  <#% @orders.each do |order| %>
    <#%= render partial: 'order', collection: @orders %>
  <#% end %>

  <%= render(@orders) || "No Orders" %>

</table>
