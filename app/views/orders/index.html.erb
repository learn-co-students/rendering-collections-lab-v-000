<h2>Open Orders</h2>
<table>
  <tr>
    <th>Order Id</th>
    <th>Product
    <th>Price
    <th>Customer</th>
  </tr>
  <% #@orders.each do |order| %>
      <tr>
        <%= render(@orders) || "No Orders"%>
      </tr>
  <% #end %>
</table>
